{
  "mcpServers": {
    "pillura-med-firebase": {
      "command": "npx",
      "args": [
        "-y",
        "firebase-tools@latest",
        "mcp",
        "--dir",
        "C:/Disk_D/flutterApp/pillura_med",
        "--only",
        "firestore,auth"
      ]
    }
  }
}
